AJS.toInit(function ($) {
    // Enable floating scrollbars for tables inside the content area
    // see: jquery.floatingScrollbar.js
    $(".wiki-content").find(".table-wrap").floatingScrollbar();
});
